#include "main.h"

/**
 * mul - function to calculate multiply of two integer
 *
 * @a: first input of function
 * @b: second input of function
 * Return: return 1 if 'c' is lowercase
 *         otherwise always 0 (Success)
*/

int mul(int a, int b)
{
	return (a * b);
}

